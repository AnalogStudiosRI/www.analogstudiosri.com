diff --git a/node_modules/@greenwood/cli/src/config/rollup.config.js b/node_modules/@greenwood/cli/src/config/rollup.config.js
index 7cc80f1..64d43e4 100644
--- a/node_modules/@greenwood/cli/src/config/rollup.config.js
+++ b/node_modules/@greenwood/cli/src/config/rollup.config.js
@@ -59,6 +59,11 @@ function greenwoodResourceLoader(compilation, browser = false) {
   return {
     name: "greenwood-resource-loader",
     async resolveId(id, importer, options) {
+      // Virtual modules do not represent files and are owned by the plugin that created them.
+      if (id.startsWith("\x00") || importer?.startsWith("\x00")) {
+        return null;
+      }
+
       const { userWorkspace, scratchDir } = compilation.context;
       const normalizedId = cleanRollupId(id);
       const importerUrl = new URL(`file://${cleanRollupId(importer) ?? ""}`);
@@ -95,6 +100,11 @@ function greenwoodResourceLoader(compilation, browser = false) {
       }
     },
     async load(id) {
+      // Let the plugin that created a Rollup virtual module load it.
+      if (id.startsWith("\x00")) {
+        return null;
+      }
+
       let idUrl = new URL(`file://${cleanRollupId(id)}`);
       const { pathname } = idUrl;
       const extension = pathname.split(".").pop();
@@ -103,7 +113,7 @@ function greenwoodResourceLoader(compilation, browser = false) {
       };
 
       // filter first for any bare specifiers
-      if ((await checkResourceExists(idUrl)) && !id.startsWith("\x00")) {
+      if (await checkResourceExists(idUrl)) {
         if (extension !== "js") {
           for (const plugin of resourcePlugins) {
             if (plugin.shouldResolve && (await plugin.shouldResolve(idUrl))) {
@@ -287,6 +297,11 @@ function greenwoodImportMetaUrl(compilation) {
     name: "greenwood-import-meta-url",
 
     async transform(code, id) {
+      // Virtual modules do not represent files and cannot be converted to file URLs.
+      if (id.startsWith("\x00")) {
+        return null;
+      }
+
       const resourcePlugins = compilation.config.plugins
         .filter((plugin) => {
           return plugin.type === "resource";
diff --git a/node_modules/@greenwood/cli/src/runtimes/node/register.js b/node_modules/@greenwood/cli/src/runtimes/node/register.js
index 907edfe..60c9a68 100644
--- a/node_modules/@greenwood/cli/src/runtimes/node/register.js
+++ b/node_modules/@greenwood/cli/src/runtimes/node/register.js
@@ -1,17 +1,27 @@
 // entry point for NodeJS custom loader hooks, e.g. `node --import @greenwood/cli/register`
 import { registerHooks } from "node:module";
-import { SHARE_ENV } from "node:worker_threads";
+import { SHARE_ENV, workerData } from "node:worker_threads";
 import { initializeSyncWorkerBridge } from "../bridge.js";
 
-registerHooks(
-  initializeSyncWorkerBridge(new URL("./worker.js", import.meta.url), {
-    workerOptions: {
-      // share read / write access to process.env across parent / child worker threads, like `__GWD_COMMAND__`
-      // https://nodejs.org/docs/latest-v24.x/api/worker_threads.html#worker_threadsshare_env
-      env: SHARE_ENV,
-      // send `[]` to prevent the initial loader hook (--import @greenwood/cli/register``) from inheriting itself as part of `process.execArgv`
-      // and thus have register calling itself recursively
-      execArgv: [],
-    },
-  }),
-);
+const GREENWOOD_LOADER_NODE_REGISTER = "greenwood-loader-node-register";
+
+// Node propagates `--import` preloads from `NODE_OPTIONS` into workers,
+// and Greenwood’s register module creates its own worker so that worker preloads the register module again and the synchronous bridge deadlocks.
+// we use a guard here to prevent the worker backing the bridge from registering another bridge when this module is preloaded through `NODE_OPTIONS`
+// https://nodejs.org/download/release/v24.13.1/docs/api/cli.html#--importmodule
+// https://github.com/ProjectEvergreen/greenwood/pull/1795
+if (workerData !== GREENWOOD_LOADER_NODE_REGISTER) {
+  registerHooks(
+    initializeSyncWorkerBridge(new URL("./worker.js", import.meta.url), {
+      workerOptions: {
+        // share read / write access to process.env across parent / child worker threads, like `__GWD_COMMAND__`
+        // https://nodejs.org/docs/latest-v24.x/api/worker_threads.html#worker_threadsshare_env
+        env: SHARE_ENV,
+        // send `[]` to prevent the initial loader hook (--import @greenwood/cli/register``) from inheriting itself as part of `process.execArgv`
+        // and thus have register calling itself recursively
+        execArgv: [],
+        workerData: GREENWOOD_LOADER_NODE_REGISTER,
+      },
+    }),
+  );
+}
\ No newline at end of file
